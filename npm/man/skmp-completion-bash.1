.nh
.TH "SKMP" "1" "Sep 2026" "skmp" "User Commands"

.SH NAME
skmp-completion-bash - Generate the autocompletion script for bash


.SH SYNOPSIS
\fBskmp completion bash\fP


.SH DESCRIPTION
Generate the autocompletion script for the bash shell.

.PP
This script depends on the 'bash-completion' package.
If it is not installed already, you can install it via your OS's package manager.

.PP
To load completions in your current shell session:

.EX
source <(skmp completion bash)
.EE

.PP
To load completions for every new session, execute once:

.SS Linux:
.EX
skmp completion bash > /etc/bash_completion.d/skmp
.EE

.SS macOS:
.EX
skmp completion bash > $(brew --prefix)/etc/bash_completion.d/skmp
.EE

.PP
You will need to start a new shell for this setup to take effect.


.SH OPTIONS
\fB-h\fP, \fB--help\fP[=false]
	help for bash

.PP
\fB--no-descriptions\fP[=false]
	disable completion descriptions


.SH SEE ALSO
\fBskmp-completion(1)\fP


.SH HISTORY
11-Sep-2026 Auto generated by spf13/cobra
